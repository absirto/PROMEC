services:
  backend:
    build:
      context: ./Backend
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - ./Backend/.env.production
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
    ports:
      - '127.0.0.1:3001:3001'
    volumes:
      - uploads_data:/app/uploads

  frontend:
    build:
      context: ./Frontend
      dockerfile: Dockerfile
      args:
        REACT_APP_API_URL: https://api.ucalcom.com/v1
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - '127.0.0.1:3000:80'

  db:
    image: postgres:15
    restart: unless-stopped
    env_file:
      - ./Backend/.env.production
    environment:
      POSTGRES_USER: postgres
      POSTGRES_DB: promec_db
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres -d promec_db']
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - db_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    restart: unless-stopped
    command: redis-server --save 60 1 --loglevel warning
    volumes:
      - redis_data:/data

volumes:
  db_data:
  redis_data:
  uploads_data:
